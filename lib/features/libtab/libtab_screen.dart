
import 'package:flutter/material.dart';

class LibTabScreen extends StatelessWidget {
  const LibTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LibTab Screen'),
      ),
    );
  }
}
